#include <stdio.h>
#define MAX_SIZE 100

int main(char* argc[], int argv){
    return 0;
}
// Push() function: 
/* Printf("Enter element to enter: ")
   scanf("%d", &x)
   if(top == MAX_SIZE-1)
   return -1
   else:
   top = top+1
   stack(top = x)*/

// Pop() function:
/*  Printf("Enter element to pop: ")
    scanf("%d", &x)
    if(top == -1)
    return -1
    else: 
    (top -1)
*/

// Peek() function:
/*

*/